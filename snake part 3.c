void logic(){

//snake tail movement and head movement with arrays(complete later)


//colision detection(complete)
if (x < 0 || x >= WIDTH || y < 0 || y >= HEIGHT)
        gameOver = 1;

   //logic for game over if tail hit is detected(unfinished)
}

